{
  "name": "@loom/mcp",
  "version": "0.3.0",
  "description": "LOOM MCP Server — Model Context Protocol server",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "loom-mcp": "bin/loom-mcp"
  },
  "scripts": {
    "build": "tsc",
    "test": "echo 'No tests for MCP server'"
  },
  "dependencies": {
    "@loom/core": "^0.3.0",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
